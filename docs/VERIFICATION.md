# 검증 기록

기존 기능 검증일: 2026-09-19. 회원 탈퇴 추가 작업 시작일: 2026-09-20. Windows 환경의 Node.js 24.19.0, pnpm 11.19.0으로 점검합니다. 실제 사용자 DB·API Key·개인 환경 파일·작업용 컴파일 산출물은 제출 소스에 포함하지 않습니다.

## 이번 회원 탈퇴 추가 작업

기준 체크포인트 `04e4852` 이후 기능 커밋 `0d554351212bf8b02571c1b6e1e072206247def7`을 공개 배포했습니다. 같은 코드베이스·SQLite 영구 볼륨·Railway 웹/워커·Brevo·인증 구조를 유지했습니다. 아래 130개 테스트와 이전 공개 서비스 기록은 탈퇴 기능 검증으로 재사용하지 않습니다.

| 항목                                 | 현재 상태                                                                                                                              |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| 스키마·삭제 범위 감사                | users의 설문/알림 설정, 사용자 FK·제품 자식, 완료/알림 기록, user_id 또는 email_hash 연결 재설정 요청과 해시 제한 버킷을 확인          |
| Brevo 사용 감사                      | 서버 코드에서 `/v3/smtp/email` 사용 확인. 별도 Contacts API 호출 없음                                                                  |
| 탈퇴 API·트랜잭션·알림 경쟁 처리     | 구현 완료. 비밀번호 확인 중 취소·동시 확인, 권한 재사용 방지와 삭제된 알림 후보 처리까지 회귀 검사                                     |
| 추가/전체 자동 테스트·기존 기능 회귀 | **155개 통과**(컴파일 러너 147·워커 8). 기존 130개 + 회원 탈퇴 19개·알림 경쟁 4개·UI 2개                                               |
| 실제 HTTP 통합                       | 최종 수정본의 새 독립 DB·실제 Next 서버에서 **8개 그룹 모두 통과**                                                                     |
| TypeScript·포맷·운영 빌드            | 최종 `next typegen`·`tsc`·전체 Prettier·운영 빌드 모두 성공. 빌드 exit code 0                                                          |
| 기존 DB 이전·보존                    | 독립 복제한 v4 DB의 기존 10개 테이블에서 모든 기존 행·열을 v5 이전 전후 비교해 동일 확인. 외래 키 검사 위반 0건                        |
| 별도 계정의 로컬 UI                  | 새 계정 → 설정 위험 영역 → 삭제 안내 → 틀린 비밀번호 400·로그인 유지 → 정상 확인 → 최종 체크 전 비활성/후 활성 → 취소·계정 유지 확인   |
| 로컬 모바일                          | 390×844 viewport에서 해당 탈퇴 흐름을 확인하고 scrollWidth 390으로 가로 넘침 없음 확인                                                 |
| 공개 HTTP 탈퇴·계정 분리             | 새 A/B 계정으로 삭제·모든 기존 세션 401·보호 페이지 5개 307·B 데이터 동일·같은 이메일의 새 ID/빈 상태 확인. A/B 정리 완료              |
| 공개 UI 탈퇴·재가입                  | 별도 C 계정의 제품·완료·보호자 설정 후 탈퇴, 이전 접근 차단, 같은 이메일 재가입·온보딩·빈 대시보드/기록/보호자 화면까지 전체 흐름 통과 |
| 기존 운영 데이터 보존                | 이전 검증 계정 3개의 데이터·세션을 읽기 전용으로 비교해 배포 후 보존 확인                                                              |
| GitHub CI·웹 배포                    | `0d55435`의 표준 CI 모든 단계 성공, Railway 웹 ACTIVE·health HTTP 200                                                                  |
| 운영 워커                            | 같은 기능 버전 ACTIVE. 관찰한 20:16~20:23 로그가 brevo checked=5, sent=0, captured=0, failed=0, skipped=5로 반복 정상                  |
| 소스·전체 이력 감사                  | 현재 소스 63개 파일에서 검사한 비밀 패턴 0건. Git 전체 12개 커밋·130개 blob 중 텍스트 128개에서 비밀·금지 파일 경로 0건                |

[회원 탈퇴 버전 GitHub Actions](https://github.com/june-young19/haru-nutri/actions/runs/35507327222)에서 표준 설치·타입·테스트·통합·빌드가 모두 성공했습니다. 공개 HTTPS의 A/B 검사는 프로필·설문·보호자·영양제·복용·보관 이력을 만든 뒤 잘못된 비밀번호·CSRF·최종 확인 누락을 거부하는지부터 확인했습니다. 탈퇴 후 다른 계정의 API 값이 동일했고 같은 이메일 재가입은 새 ID와 빈 제품·기록·보호자·설문 상태였습니다. 테스트 계정 정리를 완료했고 이 검사의 외부 이메일 발송 요청은 0건입니다.

별도 C 계정의 실제 공개 UI에서는 본인 알림을 끄고 비타민 D 10μg·09:00 제품을 등록해 복용 완료 100%를 확인한 뒤 가상 보호자 설정을 저장했습니다. 설정의 위험 영역에서 비밀번호 확인과 필수 최종 체크를 마치고 승인 후 탈퇴하자 `/login?deleted=success`로 이동했습니다. 뒤로가기도 로그인 화면을 유지했고 기존 자격으로 로그인은 거절됐으며 `/dashboard` 직접 접근도 로그인으로 이동했습니다. 같은 이메일로 새로 가입해 온보딩 → 오늘 일정 0·제품 없음·연속 기록 0 → 최근 7일 일정/완료 0·오늘 0/0 → 보호자 이메일 빈칸·알림 OFF·동의 기록 없음까지 확인했습니다. 마지막에는 로그아웃해 로그인 화면으로 돌아왔습니다. 재가입한 빈 합성 계정 1개는 유지하며 활성 제품·일정·보호자가 없습니다. HTTP 검사용 A/B는 정리 완료한 상태와 구분합니다. 개인 주소·비밀번호·인증 쿠키는 공개 기록에 남기지 않습니다.

운영 워커의 위 집계는 서비스 전체의 정상 주기 관찰이며 탈퇴한 사용자에게 5건이 남았다는 뜻이 아닙니다. 탈퇴 대상의 이후 본인·보호자 알림 제외와 발송 대기 후보 경쟁 처리는 자동 테스트로 검증했고 운영 워커의 정상 실행을 별도로 관찰했습니다. 이번 작업의 실제 Brevo 외부 발송은 0건이며 이전 메일 수신 기록을 재검증한 것으로 표시하지 않습니다.

전체 155개는 취소와 비밀번호 확인이 동시에 진행되는 경우의 회귀 검사까지 포함해 다시 실행한 결과입니다. v4 DB 검사는 원본 운영 DB가 아닌 독립 복제본에서 수행했습니다. 데이터 보존 검사는 v5 DB와 v4 전용 구버전 코드의 호환성을 의미하지 않습니다. 구버전은 스키마 버전 검사로 실행을 거절하므로 v5 호환 코드로 수정해야 합니다.

운영 DB 행 삭제, 과거 복용 이력 삭제, 이후 알림 대상 제외, 다른 사용자 보존을 각각 검사합니다. SQLite 파일의 잔여 바이트·백업·메일 공급자 로그의 물리 소거는 이 기능의 검증 대상이나 보장으로 삼지 않습니다. 기존의 소유자 없는 이메일+IP 해시 제한 버킷은 역추적 한계와 만료 후 정리 방식을 [보안 설명](SECURITY.md#회원-탈퇴와-보관-범위)에 구분했습니다.

## 이번 제품 검색·비밀번호 재설정 추가 작업

작업 전 정상 버전 체크포인트는 `b1d6bfd`이며 새 기능 버전 `c602251498c383e4616675f271ed45b991d97dcc`을 공개 배포했습니다. 같은 프로젝트·GitHub·Railway 웹/워커·SQLite 영구 볼륨·Brevo provider를 유지했습니다. 아래의 기존 73개 테스트와 복용 알림 수신 결과를 새 기능의 실제 검증으로 계산하지 않습니다.

| 항목                                      | 현재 확인 범위                                                                                                                                                        |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 의존성 설치                               | lockfile 고정·오프라인 설치 성공                                                                                                                                      |
| 자동 테스트                               | **130개 통과**: 도메인 10·UL 12·서버 38·공급자 5·워커 8·제품 17·정렬 14·재설정 19·제품 API 7                                                                          |
| 실제 HTTP 통합                            | 별도 운영 모드 Next 서버에서 기존 **7개 그룹 모두 통과**. 실제 외부 제품 API·재설정 메일 검증과 구분                                                                  |
| 제품 정렬                                 | 신규 14개 테스트 실행 및 독립 strict TypeScript 컴파일 성공. 위 단위 테스트 합계에 포함                                                                               |
| 정렬에서 확인한 항목                      | 일반 A>B>C/초과 D 분리, 실제 A·B2의 확인 필요, B군 관심 그룹, canonical 중복, 누락 함량·형태·단위·나이, 기존 성분 불확실성, 부분 합계 초과, 정렬 우선순위·입력 불변성 |
| TypeScript·포맷·운영 빌드                 | UI 간격·인증 쿠키 변경까지 포함한 최종 타입 검사·전체 Prettier 검사·운영 빌드 성공. 컴파일·타입·trace 단계 모두 정상 종료                                             |
| 로컬 개발 서버·직접 입력                  | 포트 3000 서버 실행. 실제 브라우저에서 이름·나이 가입 → 비타민 D 25μg 직접 입력 → UL 미리보기 → 저장 확인                                                             |
| 외부 설정 부족 시 처리                    | API Key가 없는 로컬 검색에서 오류 안내와 직접 입력 대안 확인. capture 모드의 비밀번호 재설정은 HTTP 503으로 거절하며 인증코드를 가짜로 발급하지 않음                  |
| 로컬 모바일                               | 390×844 viewport에서 제품 검색·비밀번호 재설정 화면의 가로 넘침 없음과 간격 수정 확인. 전체 페이지 검사로 확대하지 않음                                               |
| 공식 자료 조사                            | I0030 공식 문서의 요청 조건·필드·한도를 확인. 실제 계정 키 조회와 구분                                                                                                |
| Railway 공식 API Key·실제 조회            | 사용자가 등록한 서버 변수로 I0030 실제 조회 성공. `비타민` 총 4,860건 중 첫 100건을 수신하고 complete 4·partial 1·나머지 95건의 수동 확인 필요를 구분                 |
| 운영 설문·후보 분석                       | 서버의 설문 관심 4개로 제한된 후보에서 일반 1·확인 필요 349·주의 필요 0 확인. 전수 제품 순위가 아님                                                                   |
| 운영 제품 저장·UL·삭제                    | 실제 공식 제품의 일일 함량을 원문과 대조. 기존 제품과 합산·초과 후보 분리·확인 없는 저장 409·확인 후 201·복용 완료·삭제 후 이력 보존 확인                             |
| 공개 데스크톱 제품 UI                     | `c602251`에서 검색·상세·확인 체크·초안 가져오기·시간 선택·세 성분 UL 비교 결과·저장 버튼 활성화 확인. 사용자 초안은 저장하지 않음                                     |
| 비밀번호 재설정 실제 Brevo 메일·수신·변경 | 사용자가 공개 서비스에서 코드 메일 수신 → 직접 코드 입력 → 새 비밀번호 저장 → 새 비밀번호 로그인 성공을 확인. 인증된 공개 대시보드도 관찰                             |
| 이번 원격 CI·공개 배포                    | `c602251`의 표준 설치·타입·테스트·통합·빌드 모두 성공. 공개 `/api/health`·`/forgot-password` HTTP 200                                                                 |
| 기존 미리보기 DB 보존                     | SQLite 백업 API로 v3 DB를 독립 복제한 뒤 v4 이전. 기존 9개 테이블의 모든 행이 동일함을 확인하고 재설정용 테이블만 추가                                                |
| 새 배포 후 기존 운영 데이터 보존          | 기존 테스트 계정 2개의 세션·프로필·보호자 분리·활성 제품·삭제 제품 완료 이력 보존 검사 통과                                                                           |
| 소스·전체 Git 이력 감사                   | 10개 커밋·106개 blob 중 텍스트 104개 검사. 금지된 환경 파일·DB 경로 0개, 검사한 비밀 패턴 0건                                                                         |

DB 보존 검사는 실제 기존 미리보기 DB를 `node:sqlite`의 백업 기능으로 복제한 **별도 DB**에서 실행했습니다. `users`, `sessions`, `supplements`, `supplement_ingredients`, `intake_schedules`, `intake_records`, `guardian_settings`, `notification_logs`, `auth_attempts`의 모든 행을 이전 전후 비교했습니다. 원본 DB를 시험용으로 초기화하거나 개인정보를 공개하지 않았습니다. 이 로컬 v3→v4 이전 검사와 별도로, 새 버전 공개 배포 후 기존 두 검증 계정의 세션·프로필·보호자 분리·제품·완료 이력 유지도 확인했습니다.

새 버전의 [GitHub Actions](https://github.com/june-young19/haru-nutri/actions/runs/35440578383)에서 표준 `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration`, `pnpm build`의 모든 단계가 성공했습니다. 아래의 이전 Brevo CI 링크와 구분합니다.

Railway 웹과 기존 알림 워커 모두 `c602251`의 ACTIVE 상태를 확인했습니다. 워커는 11:52:41 UTC까지 관찰한 자동 주기에서 `mode=brevo`, `checked=1`, `sent=0`, `captured=0`, `failed=0`, `skipped=1`을 기록했습니다. 정상 주기와 실패 없음의 확인이며 이번 추가 기능 배포에서 새 복용 알림을 발송·수신한 검사로 세지 않습니다.

실제 제품 검사는 본인·보호자 알림을 끈 별도 계정에서 수행했습니다. 품목 보고번호 `202500287596`의 **3중케어 칼슘마그네슘비타민D**를 조회하고, 원문의 1일 1정(1,000mg) 및 일일 칼슘 260mg·마그네슘 105mg·비타민 D 10μg을 대조했습니다. 기존 D 20μg과 후보의 합계 30μg·중복 제품 2개를 확인했고, 기존량을 D 95μg으로 바꾼 경우 후보가 105μg 초과로 주의 필요 그룹에 분리됐습니다. 확인 없이 저장하면 409, 확인 후에는 201이 반환됐습니다. 이 실제 조회·등록 검사는 해당 제품의 구매·복용 추천이 아닙니다.

그 제품의 복용 완료 후 삭제하자 오늘 목록에서 제외되고 분석값은 기존 D 95μg으로 돌아갔으며 삭제된 제품의 완료 이력은 남았습니다. 두 테스트 제품을 모두 보관한 뒤 활성 제품 0개, 후보의 일반 그룹 복귀와 중복 없음도 확인했습니다.

공개 데스크톱 UI는 로그인한 `/supplements/new`에서 `3중케어` 검색 → 보고번호 `202500287596` 선택 → 공식 상세 확인 → 확인 체크 → 초안 가져오기 순으로 직접 검증했습니다. 제품명·제조사와 칼슘 260mg·마그네슘 105mg·비타민 D 10μg이 일치했고 복용 시간은 비어 있었습니다. 브라우저 기본 시간 선택기에 01:00을 입력한 뒤 세 성분이 등록량 기준 UL 이하로 표시되고 저장 버튼이 활성화됐습니다. 이는 실제 섭취 안전성이나 권장량 판정이 아닙니다. 사용자의 초안은 저장하지 않았고 저장·복용·삭제의 전체 흐름은 앞서 별도 계정의 공개 HTTP 검사에서 확인했습니다.

이 공개 화면 검증은 `c602251`의 데스크톱 너비 1280px에서 수행했습니다. 모바일 크기 전환 시도가 실제로 적용되지 않아 이번 공개 화면을 모바일 검증으로 계산하지 않습니다. 신규 검색·재설정 화면의 모바일 근거는 위 로컬 390×844 검사입니다. 자동화 도구의 시간 필드 직접 채우기가 React 상태에 반영되지 않았지만 기존 공개 코드에서 기본 시간 선택기·키보드 입력은 정상 동작했습니다. 이후 입력값을 상태 갱신 전에 보관하는 방어적 보강을 추가하고 로컬 운영 빌드를 다시 통과했으며, 이를 기존 공개 화면의 실패나 추가 테스트 건수로 계산하지 않습니다.

가상 테스트의 제품 A/B/C/D는 실제 시판 제품 추천이 아닙니다. 수치 UL을 지원하는 독립 성분 fixture에서 정렬을 확인했고 실제 A·B2·D·마그네슘 조합은 모호한 항목을 지우지 않고 “확인 필요”로 검증했습니다. 성분 함량·개인 이메일·API Key·인증코드를 실제 운영 데이터처럼 테스트 소스에 넣지 않습니다.

제품명 조회는 식품안전나라의 공식 API를 사용하지만, 관심 제품 정렬은 최대 4개 제품명 검색어의 첫 100건씩에서 추린 부분 집합입니다. 전수 제품 순위나 의학적 안전 검증이 아닙니다. [제품 처리 범위](PRODUCT_DATA.md)

비밀번호 재설정은 사용자가 **공개 서비스에서 Brevo 인증코드 메일 수신 → 코드 입력 → 새 비밀번호 저장 → 새 비밀번호로 로그인**의 전 과정을 성공했다고 확인했습니다. 이후 공개 `/dashboard`가 인증된 상태인 것도 확인했습니다. 인증코드·새 비밀번호는 사용자가 직접 입력했으며 공개 기록에 원문·개인 이메일·이름을 남기지 않습니다. 이전 세션 폐기는 재설정 자동 테스트에서 확인했으며 운영의 이전 세션을 직접 재사용해 검증한 것으로 표시하지 않습니다. 로컬 capture에서의 503은 외부 발송을 지원하지 않는 모드를 정직하게 거절한 결과로, 이 공개 서비스의 메일 오류가 아닙니다.

## 이전 Brevo 전환 검증 상태

이 문서는 이전 버전의 완료된 검증과 Brevo 전환 후 확인할 항목을 구분합니다. 이전 공급자의 실제 수신 기록은 Brevo의 발송·전달 성공 근거가 아닙니다. 기존 Railway 서비스·SQLite 볼륨·공개 URL을 유지하는 전환이며, 아직 관찰하지 않은 결과를 성공으로 기록하지 않습니다.

| 항목                                   | 현재 상태                                                                                                      |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Brevo 변경분의 자동 테스트             | **73개 통과**: 도메인 10·UL 12·서버 38·이메일 공급자 5·워커 8                                                  |
| Brevo 변경분의 실제 HTTP 통합          | 같은 소스 해시에서 별도 Next 서버·capture 모드의 **7개 그룹 통과**                                             |
| 의존성·TypeScript·운영 빌드            | lockfile 고정 설치, 최종 `tsc`, 최적화된 운영 빌드 모두 성공                                                   |
| 표준 GitHub CI                         | 커밋 `a711e5d9cbb04553d7dfff848cc0b407fbdd50db`의 설치·타입·테스트·통합·빌드 모두 성공                         |
| Brevo 운영 변수                        | `EMAIL_MODE=brevo`, `EMAIL_FROM_NAME=하루영양` 적용. 사용하지 않는 이전 공급자 키 삭제                         |
| Brevo 계정·API Key·확인한 발신자       | 전화·이메일 인증, Gmail Sender Verified, 서버 API Key·발신자 연결 완료                                         |
| 웹·워커의 Brevo 버전 재배포            | 해당 커밋으로 두 서비스 ACTIVE 확인                                                                            |
| 발신자 확인·실제 From                  | Brevo 로그에서 From은 `*.brevosend.com`, Reply-To는 등록한 Gmail 주소로 확인                                   |
| 본인 Gmail A1                          | 16:02 자동 발송·Delivered·기본 받은편지함 수신 확인                                                            |
| 1차 학교 이메일 B1·C1                  | 일반·보호자 각 1통 Delivered. 사용자가 Outlook에서도 찾지 못해 실제 수신 미확인, 원인 불명                     |
| 2차 본인 선행 알림 A2                  | 16:16 자동 발송·Delivered. 별도 실제 수신 확인은 요청하지 않음                                                 |
| 2차 별도 Gmail B2                      | 16:16 자동 발송·Delivered·사용자 실제 수신 확인. 폴더 미확인                                                   |
| 2차 Gmail 보호자 C2                    | 16:21 동의·추가 유예 후 자동 발송·Delivered·기본 받은편지함 수신 확인                                          |
| 재배포 후 기존 데이터 보존             | 기존 테스트 계정 2개 데이터 보존 확인                                                                          |
| Brevo 배포의 실제 웹 Restart 후 보존   | 15:54:41 KST 재기동 후 A/B 세션·프로필·보호자 분리·활성 제품·삭제 제품 완료 기록 유지                          |
| Brevo 버전 공개 기능 검사              | 알림을 끈 새 계정 2개로 가입·합산·완료·삭제 이력·계정/보호자 분리·로그아웃/재로그인 3개 그룹 통과              |
| Brevo 버전 공개 모바일 설정            | `/settings` 446px에서 이름·나이·이메일 카드, Brevo 안내·저장 버튼 시각 확인. viewport와 scrollWidth 모두 446px |
| Brevo 실제 중복 방지·삭제 후 발송 제외 | 워커 재배포 후 같은 알림 추가 발송 없음. 최종 정리 후 16:26:28 모든 집계 0                                     |
| 최신 소스·이력의 비밀 패턴 검사        | 현재 소스·4개 커밋/72개 blob·당시 수정 문서 3개에서 검사한 비밀·개인정보 패턴 0건                              |

실제 검증 시에는 `EMAIL_MODE=brevo`를 사용하고 API 오류를 캡처 성공으로 대체하지 않습니다. 각 대상의 앱 접수·공급자 이벤트·수신자 관찰을 별도로 기록하며 API Key·이메일 주소·메시지 ID는 공개하지 않습니다. 절차는 [배포 가이드](DEPLOYMENT.md)와 [시연 가이드](DEMO.md)에 있습니다.

이전 Brevo 전환 변경분은 코드·로컬 검증·원격 CI·실제 배포까지 완료했습니다. 동일 테스트 소스를 컴파일한 러너에서 도메인·UL·서버·공급자 65개를 실행했고 워커 8개를 별도로 통과해 총 73개입니다. capture HTTP 검사는 별도 3101 서버에서 수행했으며 실제 Brevo 요청을 보낸 검사가 아닙니다. 현재 환경의 자식 프로세스 제한 우회는 아래 설명과 같습니다. 전환 전 62개 기록은 과거 결과로 구분해 보존합니다.

Brevo 커밋 `a711e5d9cbb04553d7dfff848cc0b407fbdd50db`의 [GitHub Actions](https://github.com/june-young19/haru-nutri/actions/runs/35427479462)에서 표준 `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration`, `pnpm build`가 모두 성공했습니다. Railway 웹과 워커가 같은 커밋으로 ACTIVE인 상태를 확인했습니다.

전환 후 공개 HTTPS 검사 3개 그룹을 통과했습니다. 기존 테스트 계정 2개의 데이터가 유지되었고, 별도의 새 계정 2개는 본인 알림을 끈 상태에서 가입했습니다. 복수 성분을 가진 제품의 비타민 D 20+25=45μg 합산과 제품별 비타민 C 100mg, 저장·복용 완료·삭제 이력·보호자 정보의 계정 분리·로그아웃/재로그인을 확인했습니다. 기존 사용자 개인정보는 문서에 기록하지 않습니다.

Railway 웹의 실제 Restart를 실행해 2026-09-19 15:54:41 KST 재기동 로그를 확인했습니다. 이어 공개 HTTPS 보존 검사가 성공 종료하여 Brevo 검증용 A/B 계정의 기존 세션·프로필·보호자 정보 분리·활성 제품·삭제 제품 완료 이력이 유지됨을 확인했습니다.

공개 모바일 `/settings`의 실제 446px viewport에서 이름·나이·이메일 카드, Brevo 이메일 발송 안내와 저장 버튼을 육안으로 확인했습니다. DOM의 viewport 너비와 scrollWidth가 모두 446px여서 가로 넘침이 없었습니다. 사용자 설정은 변경하지 않았고 30분 유예 시간을 유지했습니다. 이번 확인은 설정 화면만의 범위이며 모든 페이지를 새로 모바일 검증했다는 뜻은 아닙니다.

Brevo 계정의 전화·이메일 인증과 Gmail Sender Verified를 확인하고 사용자가 API Key를 Railway 비밀 변수로 등록했습니다. 확인한 발신자를 웹에 적용해 재배포했고 웹·워커 모두 ACTIVE인 상태에서 자동 발송했습니다. 설정 전의 HTTP 503은 키 누락을 숨기지 않는 동작을 확인한 과거 검사이며 현재 상태가 아닙니다. 실제 발송 결과는 아래에 구분합니다.

## 전환 전 이름·나이·UL·삭제 검증

Brevo 전환 전 이름·나이·공식 UL 비교·삭제 정책을 적용한 코드에서 아래 항목을 실행했습니다. 브라우저 시연은 기존 미리보기 DB에 별도 테스트 계정을 만들어 수행했고 기존 개인 계정은 변경하지 않았습니다. 자동 테스트·HTTP 통합 검사·당시 이메일 점검은 각각 별도 DB를 사용했습니다.

| 항목                 | 상태                                                                                                                                   |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 의존성 설치          | pnpm lockfile 고정·오프라인 설치 통과                                                                                                  |
| 운영 의존성 감사     | `pnpm audit --prod`에서 알려진 취약점 없음. 검사 시점의 공급자 데이터 기준                                                             |
| TypeScript           | `next typegen`, `tsc --noEmit` 통과                                                                                                    |
| 자동 테스트          | **62개 통과**: 도메인 10개, UL 분석 12개, 서버 34개, 워커 6개                                                                          |
| 실제 HTTP 통합       | **확장된 7개 시나리오 그룹 통과**: 이름·나이 가입, 저장·검증, UL, 복용·기록, 계정 분리·보안, 알림·동의, 삭제·재로그인·서버 페이지 보호 |
| 이름·나이 가입       | 신규 이름·나이 입력·범위 검사, 계정 응답과 재로그인 후 유지 확인                                                                       |
| 기존 DB 이전         | v1 DB fixture를 실제 이전해 이름·나이 NULL·기존 계정·기록 보존 확인                                                                    |
| 기존 미리보기 데이터 | 기존 DB 백업 후 관련 8개 테이블의 데이터 보존 확인                                                                                     |
| 공식 UL              | 연령 구간, 단위 환산, D의 IU, 미설정·판단 불가 상태, 공식 출처·경계값 확인                                                             |
| 저장 전 미리보기     | 현재·추가·합계, 수정 대상 제외, 초과 시 확인 없는 POST/PUT 409 및 확인 후 저장 검증                                                    |
| 제품 삭제            | 미완료·완료 제품 모두 오늘 목록·진행률·분석에서 제외, 이력은 보관, 재로그인 후 유지                                                    |
| 프로필 설정          | 브라우저에서 이름과 나이를 수정하고 새로고침 후 유지 확인                                                                              |
| 최종 운영 빌드       | 서버 페이지 보호·로그아웃 세션 검증·워커 개선을 포함한 최종 빌드와 TypeScript 검사 통과                                                |
| standalone 운영 실행 | `scripts/start.mjs`로 독립 DB·포트 3102에서 실제 실행. 이후 추가된 보호 페이지 인증 처리는 최신 HTTP 통합 검사로 확인                  |
| 포맷 검사            | Prettier 검사 통과. 별도 lint 스크립트는 없으며 ESLint 실행으로 표시하지 않음                                                          |
| 모바일               | 390px 화면에서 가로 넘침 없음 및 실제 스크린샷 시각 점검                                                                               |
| 비밀·데이터 제외     | Git ignore 규칙에서 개인 환경 파일·DB·백업 제외, `.env.example` 포함 확인. 제출 소스 44개 파일에서 실제 키·키 패턴 검색 결과 0건       |

`tests/integration.ts`는 별도 임시 DB와 capture 모드를 사용하며 자체 개발 서버를 시작·정리하는 구조입니다. 외부 이메일의 실제 발송·수신 검증과 구분합니다.

## 전환 전 외부 배포와 추가 검증

2026-09-19에 서버의 보호 페이지 접근 검사와 세션 처리 회귀 검사를 추가하고 **전체 62개 테스트를 다시 통과**했습니다. `tests/worker.test.ts`의 6개는 이 합계에 포함됩니다. 운영 URL·시크릿 검증, 인증 헤더와 리다이렉트 차단, 집계만 로그에 출력, 느린 스캔 비중첩, HTTP/네트워크 오류 재시도, 타임아웃, 종료 시 요청 취소를 확인했습니다. 최종 운영 빌드와 TypeScript 검사도 통과했습니다.

이 환경의 Node.js 24에서는 `node --test --experimental-test-isolation=none tests/worker.test.ts`로 자식 프로세스 없이 직접 실행했습니다. 일반 환경에서는 기존 `pnpm test`에 자동 포함됩니다. 외부 네트워크나 실제 메일을 사용하는 테스트가 아닙니다.

[GitHub 저장소](https://github.com/june-young19/haru-nutri)에 전체 소스 46개 파일을 업로드했습니다. 커밋 `141bce44b2299a5267aa7a0bf06a12c18e73c288`의 [GitHub Actions CI](https://github.com/june-young19/haru-nutri/actions/runs/35424692457)는 **성공**입니다. 표준 `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration`, `pnpm build` 단계가 모두 성공한 것을 원격으로 확인했습니다.

Railway의 웹 `haru-nutri`와 워커 `haru-reminders`는 ONLINE이며 실제 클라우드 Docker 빌드와 실행에 성공했습니다. 웹에는 500MB 볼륨을 `/app/data`에 연결했습니다. 실제 서비스는 [https://haru-nutri-production.up.railway.app](https://haru-nutri-production.up.railway.app)입니다. 배포 절차는 [DEPLOYMENT.md](DEPLOYMENT.md)에 있습니다.

공개 HTTPS 주소에서 별도 검증 **3개 그룹을 모두 통과**했습니다. 테스트 계정 A/B의 본인 알림을 끈 상태에서 실제 가입, 비타민 D 20+25=45μg 미리보기·저장, 복용 완료, 제품 삭제 후 이력 보존, 제품·보호자 정보의 계정 간 격리, 로그아웃 세션 무효화와 재로그인을 확인했습니다. 이 검사는 운영 서비스에서 실행되었으며 기존 사용자 정보를 변경하지 않았습니다.

실제 브라우저에서는 공개 랜딩·가입 페이지, 사용자의 직접 가입, 설정의 실제 이메일 모드 표시, 테스트 제품 등록과 대시보드 반영을 확인했습니다. 390px 화면에서 대시보드·설정의 가로 넘침이 없었고 로그아웃 버튼도 시각적으로 확인했습니다. 사용자의 개인 이름·나이·이메일은 이 문서에 기록하지 않습니다.

Railway 웹 서비스를 실제로 Restart하여 2026-09-19 14:54:26에 같은 볼륨이 다시 마운트되고 14:54:32에 Ready가 되는 로그를 확인했습니다. 재시작 후 공개 HTTPS 검사에서 기존 A/B 세션, 프로필, 보호자 정보의 계정 간 격리, 활성 제품, 삭제한 제품과 활성 제품의 완료 이력이 **모두 유지**되는 것을 확인했습니다. 영구 볼륨을 설정했다는 사실과 실제 보존 검증을 구분해 수행했습니다.

전환 전 워커의 연속된 정상 검사 주기와 같은 알림을 반복 생성하지 않는 동작을 확인했습니다. 이 결과를 새 공급자의 API·멱등성·전달 동작 검증으로 간주하지 않습니다.

확장된 HTTP 통합 검사는 독립 DB와 capture 모드에서 모두 통과했습니다. 원래 7개 그룹의 마지막 그룹을 확장해 비로그인·만료·로그아웃 세션의 보호 페이지 접근 제한, 계정 A/B의 제품과 보호자 정보 분리, 삭제 상태 유지, DB 오류가 잘못된 로그인 오류 대신 서버 오류로 처리되는 동작을 포함합니다. 그룹 수를 추가 테스트 개수와 혼동해 늘려 표시하지 않습니다.

## 브라우저에서 직접 확인한 순서

추가로 같은 로컬 브라우저에서 A 계정 전용 제품을 저장한 뒤 설정에서 로그아웃했습니다. 뒤로가기는 `/login`을 유지했고 `/dashboard` 직접 접근도 `/login`으로 이동했습니다. 이어서 B 계정으로 신규 가입하자 B의 이름·오늘 0건이 표시되고 A의 제품은 보이지 않아, 실제 화면에서도 로그아웃과 계정 분리를 확인했습니다.

1. 이름·만 나이 20세로 회원가입.
2. 종합비타민의 C 100mg·D 20μg과 별도 Vitamin D 25mcg을 등록하고, 저장 전 두 제품 합계 45μg 확인.
3. 별도 입력에서 총량 145μg의 초과 미리보기 확인. 확인 체크 전 저장 비활성, 체크 후 활성, 입력 변경 후 재검사 요구 확인.
4. 최종 등록량을 45μg으로 저장하고 성분 분석 화면과 대조.
5. 두 제품 모두 복용 완료 후 비타민 D 제품 삭제. 오늘 항목 1개, 새로고침 후 유지, 분석 합계 20μg·중복 0개 확인.
6. 복용 기록 화면에서 삭제된 제품의 완료 이력 보존 확인.
7. 이름·나이를 21세로 수정하고 재조회 후 유지 확인.
8. 로컬 알림을 점검해 남아 있는 제품만 대상으로 한 알림 1개 생성 확인.
9. 만 3세로 프로필을 바꾸고 예시 초안의 아연 10mg이 연령별 UL 7mg을 초과해 확인이 필요한 흐름을 확인.
10. 만 20세로 되돌린 후 모바일에서 로그아웃·재로그인하고 오늘 항목 1개 및 삭제 상태 유지 확인.

대시보드의 예시 버튼은 제품 3개를 자동으로 만드는 방식에서 **종합비타민 초안 → 공통 성분 검사·확인 → 저장** 흐름으로 변경했습니다. 따라서 어린 연령의 UL을 초과하는 예시도 확인 절차를 거칩니다.

## 실제 Brevo 자동 이메일 검증

2026-09-19 한국 시간 기준입니다. 1차 학교 이메일 검사와 2차 별도 Gmail 검사를 구분합니다. **총 6통 모두 실제 Railway 워커의 자동 주기로 발송되어 API 접수와 Brevo `Delivered`를 확인했습니다.** preview나 cron API를 직접 호출하지 않았으며 캡처로 대체하지 않았습니다. 사용자 실제 수신 확인은 A1·B2·C2의 세 건입니다.

| 검사·대상                      | 워커·앱 결과                                                       | Brevo 결과      | 수신자 확인                               |
| ------------------------------ | ------------------------------------------------------------------ | --------------- | ----------------------------------------- |
| 1차 A1 — 본인 Gmail            | 16:02:44 `checked=1`, `sent=1`, `captured=0`, `failed=0`           | 16:02 Delivered | 기본 받은편지함 수신 확인                 |
| 1차 B1 — 학교 이메일 일반 알림 | 16:07:23 자동 발송, 앱 sent·공급자 식별자 저장                     | 16:07 Delivered | Outlook에서도 찾지 못함. 실제 수신 미확인 |
| 1차 C1 — 학교 이메일 보호자    | 16:08:24 본인 알림 이후 동의·임시 추가 유예 5분을 거쳐 sent        | 16:08 Delivered | Outlook에서도 찾지 못함. 실제 수신 미확인 |
| 2차 A2 — 본인 선행 알림        | 16:16:26 B2와 함께 `checked=2`, `sent=2`, `captured=0`, `failed=0` | 16:16 Delivered | 별도 수신 확인을 요청하지 않음            |
| 2차 B2 — 별도 Gmail 일반 알림  | 16:16:26 A2와 같은 주기에서 자동 발송                              | 16:16 Delivered | 사용자 실제 수신 확인. 분류 폴더 미확인   |
| 2차 C2 — 해당 Gmail 보호자     | 앱 16:21:26 sent, 워커 16:21:27 sent=1. 동의·추가 유예 5분 적용    | 16:21 Delivered | 사용자 기본 받은편지함 수신 확인          |

학교 B1/C1은 사용자가 Outlook에서도 찾지 못했으며 원인은 확인되지 않았습니다. 이 두 건은 실제 수신 성공으로 표시하지 않습니다. 이후 별도 Gmail B2/C2에서 일반 알림·보호자 알림의 실제 수신을 확인했습니다. Brevo의 Opened 이벤트는 사용자의 직접 확인을 대신하는 근거로 사용하지 않았습니다. 특정 학교·개인 주소는 기록하지 않습니다.

무료 Gmail 발신자는 로그에서 `*.brevosend.com` 주소로 대체됐고 Reply-To는 원래 등록 주소였습니다. 공식 트랜잭션 문서의 예시인 `*.t-sender-sib.com`과 다를 수 있어 실제 관찰 결과를 기록합니다. 개인 주소·계정 번호·공급자 식별자는 공개하지 않습니다.

워커 재배포 후 A1의 동일 알림 추가 발송이 없었고, 2차 C2 이후에도 16:25까지 `sent=0`을 확인했습니다. 1차 정리 후 16:10:25 대상이 0이 된 뒤 별도 2차 일정을 만들었으므로 A2는 A1의 중복 발송이 아닙니다.

최종적으로 임시 보호자 설정은 알림 끄기·주소 비우기·추가 유예 120분으로 복원하고 A2/C2 테스트 제품을 UI에서 삭제해 빈 목록을 확인했습니다. 외부 B1/B2 계정은 본인·보호자 알림을 끄고 주소 비우기·120분 복원·제품 보관·로그아웃을 완료했습니다. 본인 유예 시간은 전체 검사 중 30분을 유지했습니다. **16:26:28 최종 워커는 `mode=brevo`, `checked=0`, `sent=0`, `captured=0`, `failed=0`, `skipped=0`**으로 검사 종료 후 알림 대상이 없음을 확인했습니다.

API 접수는 수신함 도착이 아닙니다. 공급자 `Delivered` 역시 수신 서버 전달 상태이며 사용자가 읽었다는 뜻은 아닙니다. 이메일 주소·API Key·공급자 메시지 ID·SMTP 고유 ID는 공개 기록에 넣지 않습니다. [Brevo 이벤트 안내](https://help.brevo.com/hc/en-us/articles/35699922048146-View-and-export-your-event-logs)

공급자는 `Delivered` 이후에도 기관의 수신 필터·지연 반송·스팸 분류 등으로 사용자가 찾지 못할 수 있다고 안내합니다. 학교 B1/C1의 원인이 그것이라고 확정한 것은 아닙니다. [공식 문제 해결 안내](https://help.brevo.com/hc/en-us/articles/17677373572626-FAQs-Why-emails-are-marked-delivered-but-are-not-received)

자동화 테스트의 캡처·모의 HTTP 결과와 이전 공급자의 수신 기록은 위 실제 Brevo 검증 건수에 포함하지 않습니다.

## 환경 제약과 재현 방법

이 실행 환경에서는 Node 자식 프로세스 생성이 `spawn EPERM`으로 제한되어 표준 `pnpm test`가 그대로 실행되지 않습니다. 동일한 테스트 소스를 작업용 폴더에 컴파일한 후 Node 테스트 러너의 프로세스 격리를 끄는 방식으로 실행했습니다. **전환 전 검증은 62개**, **Brevo 전환 후 검증은 동일 방식의 65개와 별도 워커 8개를 합쳐 73개**를 통과했습니다. 실제 HTTP 테스트는 같은 `tests/integration.ts`의 검증 본문을 별도 Next 서버와 독립 DB에 연결해 실행했습니다. 이 과정에서 테스트용 파일을 제출 폴더에 추가하지 않았습니다.

제품 검색·비밀번호 재설정 추가 작업에서는 먼저 129개를 통과했고, 최종 소스에서 새 인증 쿠키 검사 1개를 포함한 재설정 19개·제품 API 7개를 다시 실행해 총 **130개**의 성공을 확인했습니다. 재실행한 26개를 중복 합산하지 않았습니다. 기존 HTTP 검증 7개 그룹은 별도 운영 모드 Next 서버에서 통과했습니다. 마지막 변경분의 전체 포맷 검사·타입 검사·운영 빌드도 성공했습니다. 이후 `c602251`의 원격 표준 CI와 공개 배포도 성공했으며 로컬 실행 환경의 우회와 별도로 기록합니다.

이 환경에서는 설치 시 esbuild postinstall을 생략하는 우회를 사용했습니다. 일반 PC의 표준 설치·실행 안내와 구분하며, 이 제약을 이유로 타입 검사나 테스트 본문의 검증을 생략하지 않았습니다.

GitHub CI에서는 위의 표준 설치·검사·테스트·빌드 명령을 실제로 모두 통과했습니다. 일반 개발 PC에서도 README의 명령을 사용합니다. 통합 테스트는 포트 3101에서 자체 개발 서버와 임시 DB를 생성·정리하므로 같은 프로젝트의 개발 서버를 먼저 종료하세요.

빌드 설정은 TypeScript API와 worker threads를 사용하고 Webpack으로 일관되게 컴파일합니다. 검사를 끄거나 TypeScript 오류를 무시하지 않습니다.

운영 시작 스크립트에서 점검한 경로는 `/`, `/signup`, `/login`, `/dashboard`, `/supplements/new`, `/duplicates`, `/history`, `/settings`, `/settings/guardian`, `/api/health`, `/icon.svg`입니다. 최초 점검의 HTTP 200은 당시 라우트 제공 확인입니다. 이후 서버 인증 보호를 추가했으므로 현재 보호 페이지는 비로그인 요청에서 로그인으로 이동해야 하며, 모든 경로가 비로그인 상태에서 HTTP 200이라고 해석하지 않습니다. 최신 접근 제한·세션·로그인·저장 동작은 별도 HTTP 통합 검사로 확인했습니다.

## GitHub 원격 이력 검사

제품 검색·비밀번호 재설정 버전 배포 시 로컬 소스와 전체 **10개 커밋·106개 blob 중 텍스트 104개**를 검사했습니다. 금지된 비공개 환경 파일·DB 경로는 **0개**, 검사한 비밀 패턴은 **0건**이었습니다. 이는 해당 시점의 경로·정규식 검사이며 모든 가능한 비밀 형식의 부재를 보증하지 않습니다. 이후 문서·소스 커밋은 다시 검사해야 합니다.

이전 Brevo 전환 후 소스와 전체 **4개 커밋·72개 blob**, 당시 수정 중인 문서 3개를 추가 검사해 검사 대상 비밀값·개인정보 패턴은 **0건**이었습니다. 키 값·개인 주소·메시지 ID는 검사 보고서나 공개 문서에 남기지 않습니다. 아래는 최초 버전의 별도 감사 기록입니다.

커밋 `141bce44b2299a5267aa7a0bf06a12c18e73c288`까지의 전체 **2개 커밋**을 읽기 전용 GitHub API로 독립 검사했습니다. 모든 트리의 파일 경로에서 비공개 `.env`·DB·백업 파일은 **0개**였고 `.env.example`만 허용했습니다. 역사상의 고유 blob 47개 중 텍스트 45개에서 당시 메일 공급자·GitHub·OpenAI·AWS 키 접두부와 PEM 개인 키 헤더 패턴은 **0건**입니다. PNG 2개는 경로만 검사했으며 텍스트 커넥터 제한으로 바이너리 내용 검사는 제외했습니다. 개인 `.env`를 읽거나 실제 키와 대조하지 않았습니다.

이 결과는 해당 커밋까지의 경로·정규식 검사 범위이며 모든 가능한 비밀 형식의 부재를 보증하지 않습니다. 이후 문서나 코드 커밋은 새 커밋을 기준으로 다시 검사해야 합니다.

## 검증 범위의 제한

- 제품 검색·비밀번호 재설정 버전은 130개 테스트·7개 로컬 HTTP 그룹·표준 CI·공개 배포와 실제 공식 API 조회·합산·저장·삭제 보존을 확인했습니다. 실제 재설정 메일 수신·코드 확인·비밀번호 변경·새 로그인은 사용자 확인과 인증된 공개 화면으로 검증했으며, 운영 이전 세션 폐기 관찰이나 공급자의 별도 전달 이벤트 확인까지 수행했다고 확대하지 않습니다.
- Brevo 변경분은 73개 자동 테스트·7개 HTTP 그룹·TypeScript·빌드·표준 GitHub CI·웹/워커 배포·공개 기능 검사 3개 그룹이 성공했습니다. 실제 자동 메일 6통 모두 API 접수·Delivered를 확인했고 A1·B2·C2는 사용자 실제 수신도 확인했습니다. 학교 B1/C1은 실제 수신 미확인·원인 불명이며 A2는 별도 수신 확인을 요청하지 않았습니다.
- Railway의 실제 Docker 빌드·실행은 성공했습니다. 로컬에는 Docker 실행기가 없어 `docker compose` 자체를 로컬에서 실행하지는 않았습니다.

`capture`는 외부 발송이 아닙니다. 앱의 `sent`는 Brevo API 요청 접수이며 사용자 받은편지함 확인은 아닙니다. 배포 시 발신자·수신자·HTTPS·영구 디스크·워커 설정을 다시 점검하세요.
